/**
 * created 11/26/2014
 * 
 * @author arakovskyux
 * @description
 * @generated by TemplateWizard, v.2013/02/27 //do not remove this comment please
 */
define([
	"dojo/_base/declare",
	"dojo/_base/array"
], function(declare, array) {
	return declare(null, {
		// summary:
		//		Mixin for RangeBoundTextBox widgets which enables to define multiple intervals in constraints.
		//
		// description:
		//		Use this mixin if you want to define more intervals in constraints (not only 'min', 'max')
		//		Intervals are defined as array of tuples.
		//
		// example:
		//		<d:NumberTextBox dojoMixins="gjax/form/_MultiIntervalMixin" constraints="{intervals:[[-20,-10],[10,20]]}" />

		rangeCheck : function(/*Number*/primitive, /*dijit/form/RangeBoundTextBox.__Constraints*/constraints) {

			var intervals = constraints.intervals;
			if (!intervals) {
				return this.inherited(arguments);
			}

			return array.some(intervals, function(interval) {
				var min = interval[0], max = interval[1];
				return this.compare(primitive, min) >= 0 && this.compare(primitive, max) <= 0;
			}, this);
		}
	});
});